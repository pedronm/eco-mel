import 'package:flutter/material.dart';

class TransferirScreen extends StatelessWidget {
  const TransferirScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
