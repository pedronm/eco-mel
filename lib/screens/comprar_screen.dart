import 'package:flutter/cupertino.dart';

class ComprarScreen extends StatelessWidget{

  ComprarScreen({Key? key}): super(key: key);

  @override
  Widget build(BuildContext context){
    return Container(
      child: Text('Essa é a tela de compra!')
    );
  }
}